{
  "name": "ais-relay",
  "version": "1.0.0",
  "description": "AISstream.io WebSocket relay — bridges live AIS data to HTTP for Vercel",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "ws": "^8.17.0"
  }
}
