services:
  - type: web
    name: ais-relay
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: AISSTREAM_API_KEY
        sync: false
    healthCheckPath: /
    autoDeploy: true
